// Write a function sum() to calculate the sum of the first two integers and a function subtract(),
//  which subtracts the result of the function sum() and the third integer.

function matoperations(num1, num2, num3) {
            goal= adition(num1, num2)
            result = subtract(goal, num3)    
        function  adition(a,b)
          { 
            return a+b;
          };
        function subtract(c, d) { 
            return c-d
            
        } 
      
          console.log(subtract(adition(num1, num2), num3));
        
   
}
        matoperations(5,5 ,2)


    