function smallestOfThree(num1, num2, num3) {
    let num = Math.min(num1, num2, num3)
   console.log(num);
    console.log();
    
}
smallestOfThree(3,2,6);

// const points = [40, 100, 1, 5, 25, 10];

// // Sort the numbers in ascending order
// points.sort(function(a, b){return a-b});

// let lowest = points[0];

// console.log(lowest);